# mkinitcpio preset file for the 'linux-lts' package

# ALL_config="/etc/mkinitcpio.conf"
ALL_kver="/boot/vmlinuz-linux-lts"

##
PRESETS=('minimal') # 'default' 'fallback'
##

# default_config="/etc/mkinitcpio.conf"
# default_image="/boot/initramfs-linux-lts.img"

# fallback_config="/etc/mkinitcpio.conf"
# fallback_image="/boot/initramfs-linux-lts-fallback.img"
# fallback_options="-S autodetect"

minimal_config="/etc/mkinitcpio_minimal.conf"
minimal_image="/boot/initramfs-linux-lts-minimal.img"


# _options specify additional hooks to be used

# Don't forget to enable boot entry when enabling preset
